c++的多态性就是通过晚绑定技术来实现的。 c++的多态性用一句话概括就是:在基类的函数前加上virtual关
键字，在派生类中重写该函数，运行时将会根据对象的实际类型来调用相应的函数，如果对象类型是派生类，
就调用派生类的函数，如果对象类型是基类，就调用基类的函数
注意：父类的构造方法中调用虚函数，不会发生多态，还有，如果一个函数在父类中没有virtual关键字，但是
在子类中有，编译器是把它当作普通函数。而不会去查找虚函数表。

 

