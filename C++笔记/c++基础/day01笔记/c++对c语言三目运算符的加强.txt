默认情况下，c语言中，三目运算符只能当作右值，
如：
 int a=10;
 int b=20;
 int c=0;
 c=a>b?a:b;
 printf("c=%d",c);  //ok,c=20

如果修为：
（a>b?a:b）=50 //报错，因为c语言中三目运算符返回的是比较后的值（20）
如果要将三目运算符放在“=”的左边，必须这样子写：
*(a < b ? &a : &b) = 50;

而在c++中，（a>b?a:b）=50 是完全合法的，因为c++语言中三目运算符返回的是比较后
的变量（b）